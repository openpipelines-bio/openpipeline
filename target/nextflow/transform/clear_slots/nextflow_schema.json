{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "clear_slots",
  "description": "Clear one or more AnnData slot containers from a modality in an h5mu file.\n",
  "type": "object",
  "$defs": {
    "arguments": {
      "type": "object",
      "title": "Arguments",
      "description": "No description",
      "properties": {
        "input": {
          "type": "string",
          "description": "Input h5mu file.",
          "format": "path",
          "exists": true,
          "help_text": "type: `string`, multiple: False, required, direction: `input`, example: `input.h5mu`"
        },
        "modality": {
          "type": "string",
          "default": "rna",
          "description": "Which modality from the input MuData file to process.",
          "help_text": "type: `string`, multiple: False, default: `rna`"
        },
        "slots": {
          "type": "array",
          "description": "Names of the AnnData slot families to clear from the selected modality.\n",
          "items": {
            "type": "string",
            "enum": [
              "obsm",
              "varm",
              "obsp",
              "varp",
              "uns",
              "layers"
            ]
          },
          "help_text": "type: `array`, multiple: True, required, choices: `obsm`|`varm`|`obsp`|`varp`|`uns`|`layers`"
        },
        "output": {
          "type": "string",
          "default": "output.h5mu",
          "description": "Output h5mu file.",
          "format": "path",
          "exists": true,
          "help_text": "type: `string`, multiple: False, required, default: `output.h5mu`, direction: `output`"
        },
        "output_compression": {
          "type": "string",
          "description": "Compression format to use for the output AnnData and/or Mudata H5 files.\nBy default no compression is applied.\n",
          "enum": [
            "gzip",
            "lzf"
          ],
          "help_text": "type: `string`, multiple: False, example: `gzip`, choices: `gzip`|`lzf`"
        }
      },
      "required": [
        "input",
        "slots",
        "output"
      ]
    },
    "nextflow input-output arguments": {
      "type": "object",
      "title": "Nextflow input-output arguments",
      "description": "Input/output parameters for Nextflow itself. Please note that both publishDir and publish_dir are supported but at least one has to be configured.",
      "properties": {
        "publish_dir": {
          "type": "string",
          "description": "Path to an output directory.",
          "format": "path",
          "help_text": "type: `string`, required, direction: `output`, example: `output/`"
        }
      },
      "required": [
        "publish_dir"
      ]
    }
  },
  "allOf": [
    {
      "$ref": "#/$defs/arguments"
    },
    {
      "$ref": "#/$defs/nextflow input-output arguments"
    }
  ]
}
