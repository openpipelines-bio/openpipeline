  docker.enabled = true
  def viash_temp = System.getenv("VIASH_TEMP") ?: "/tmp/"
  docker.runOptions = "-i -v ${baseDir}:${baseDir} -v $viash_temp:$viash_temp"
  process.container = "dataintuitive/viash"
  params {
    convert_bdrhap_to_h5ad__input = "viash_no_value"
    convert_bdrhap_to_h5ad__id = "no_default_value_configured"
    convert_bdrhap_to_h5ad__output = "viash_no_value"
    id = ""
    testScript = ""
    testResources = [  ]
    convert_bdrhap_to_h5ad {
      name = "convert_bdrhap_to_h5ad"
      container = "convert_convert_bdrhap_to_h5ad"
      containerTag = "main_build"
      containerRegistry = "ghcr.io"
      containerOrganization = "openpipelines-bio"
      command = "convert_bdrhap_to_h5ad"
      tests {
        isDefined = false
        testScript = "NA"
        testResources = [  ]
      }
      arguments {
        input {
          name = "input"
          otype = "--"
          required = true
          type = "file"
          direction = "Input"
          multiple = true
          multiple_sep = ":"
          value = "${params.convert_bdrhap_to_h5ad__input}"
          example = "input_dir"
          description = "One or more folders containing the output of the BD Rhapsody workflow. If multiple are given, they are all combined into one h5ad file."
        }
        id {
          name = "id"
          otype = "--"
          required = false
          type = "string"
          direction = "Input"
          multiple = true
          multiple_sep = ":"
          value = "${params.convert_bdrhap_to_h5ad__id}"
          example = "my_id"
          description = "A vector of ids with same length as --input."
        }
        output {
          name = "output"
          otype = "--"
          required = true
          type = "file"
          direction = "Output"
          multiple = false
          multiple_sep = ":"
          value = "${params.convert_bdrhap_to_h5ad__output}"
          example = "output.h5ad"
          description = "Output h5ad file."
        }
      }
    }
  }