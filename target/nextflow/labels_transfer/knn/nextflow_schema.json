{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "knn",
  "description": "This component performs label transfer from reference to query using a K-Neirest Neighbors classifier.\n",
  "type": "object",
  "$defs": {
    "input dataset (query) arguments": {
      "type": "object",
      "title": "Input dataset (query) arguments",
      "description": "No description",
      "properties": {
        "input_obsm_distances": {
          "type": "string",
          "description": "The `.obsm` key of the input (query) dataset containing pre-calculated distances. \nIf not provided, the distances will be calculated using PyNNDescent.\nMake sure the distance matrix contains distances relative to the reference dataset and were obtained in the same way as the reference embedding.\n",
          "help_text": "type: `string`, multiple: False, example: `bbknn_distances`"
        }
      }
    },
    "reference dataset arguments": {
      "type": "object",
      "title": "Reference dataset arguments",
      "description": "No description",
      "properties": {
        "reference_obsm_distances": {
          "type": "string",
          "description": "The `.obsm` key of the reference dataset containing pre-calculated distances. \nIf not provided, the distances will be calculated using PyNNDescent.\n",
          "help_text": "type: `string`, multiple: False, example: `bbknn_distances`"
        }
      }
    },
    "outputs": {
      "type": "object",
      "title": "Outputs",
      "description": "No description",
      "properties": {
        "output": {
          "type": "string",
          "description": "The query data in .h5mu format with predicted labels transferred from the reference.",
          "format": "path",
          "exists": true,
          "help_text": "type: `string`, multiple: False, required, direction: `output`"
        },
        "output_obs_predictions": {
          "type": "array",
          "description": "In which `.obs` slots to store the predicted information.\nIf provided, must have the same length as `--reference_obs_targets`.\nIf empty, will default to the `reference_obs_targets` combined with the `\"_pred\"` suffix.\n",
          "items": {
            "type": "string"
          },
          "help_text": "type: `array`, multiple: True"
        },
        "output_obs_probability": {
          "type": "array",
          "description": "In which `.obs` slots to store the probability of the predictions.\nIf provided, must have the same length as `--reference_obs_targets`.\nIf empty, will default to the `reference_obs_targets` combined with the `\"_probability\"` suffix.\n",
          "items": {
            "type": "string"
          },
          "help_text": "type: `array`, multiple: True"
        },
        "output_compression": {
          "type": "string",
          "description": "Compression format to use for the output AnnData and/or Mudata H5 files.\nBy default no compression is applied.\n",
          "enum": [
            "gzip",
            "lzf"
          ],
          "help_text": "type: `string`, multiple: False, example: `gzip`, choices: `gzip`|`lzf`"
        }
      },
      "required": [
        "output"
      ]
    },
    "knn label transfer arguments": {
      "type": "object",
      "title": "KNN label transfer arguments",
      "description": "No description",
      "properties": {
        "weights": {
          "type": "string",
          "default": "uniform",
          "description": "Weight function used in prediction. Possible values are:\n- `uniform` - all points in each neighborhood are weighted equally \n- `distance` - weight points by the inverse of their distance\n- `gaussian` - weight points by the sum of their Gaussian kernel similarities to each sample\n",
          "enum": [
            "uniform",
            "distance",
            "gaussian"
          ],
          "help_text": "type: `string`, multiple: False, default: `uniform`, choices: `uniform`|`distance`|`gaussian`"
        },
        "n_neighbors": {
          "type": "integer",
          "default": 15,
          "description": "The number of neighbors to use in k-neighbor graph structure used for fast approximate nearest neighbor search with PyNNDescent. \nLarger values will result in more accurate search results at the cost of computation time.\n",
          "help_text": "type: `integer`, multiple: False, default: `15`"
        }
      }
    },
    "nextflow input-output arguments": {
      "type": "object",
      "title": "Nextflow input-output arguments",
      "description": "Input/output parameters for Nextflow itself. Please note that both publishDir and publish_dir are supported but at least one has to be configured.",
      "properties": {
        "publish_dir": {
          "type": "string",
          "description": "Path to an output directory.",
          "format": "path",
          "help_text": "type: `string`, required, direction: `output`, example: `output/`"
        }
      },
      "required": [
        "publish_dir"
      ]
    }
  },
  "allOf": [
    {
      "$ref": "#/$defs/input dataset (query) arguments"
    },
    {
      "$ref": "#/$defs/reference dataset arguments"
    },
    {
      "$ref": "#/$defs/outputs"
    },
    {
      "$ref": "#/$defs/input dataset (query) arguments"
    },
    {
      "$ref": "#/$defs/reference dataset arguments"
    },
    {
      "$ref": "#/$defs/knn label transfer arguments"
    },
    {
      "$ref": "#/$defs/nextflow input-output arguments"
    }
  ]
}
