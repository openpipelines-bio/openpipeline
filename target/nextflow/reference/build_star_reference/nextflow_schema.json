{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "build_star_reference",
  "description": "Create a reference for STAR from a set of fasta files.",
  "type": "object",
  "$defs": {
    "input_output": {
      "type": "object",
      "title": "Input/Output",
      "description": "No description",
      "properties": {
        "genome_fasta": {
          "type": "array",
          "description": "The fasta files to be included in the reference. Corresponds to the --genomeFastaFiles argument in the STAR command.",
          "format": "path",
          "exists": true,
          "items": {
            "type": "string"
          },
          "help_text": "type: `array`, multiple: True, required, direction: `input`, example: `chr1.fasta;chr2.fasta`"
        },
        "transcriptome_gtf": {
          "type": "string",
          "description": "Specifies the path to the file with annotated transcripts in the standard GTF\nformat. STAR will extract splice junctions from this file and use them to greatly improve\naccuracy of the mapping. Corresponds to the --sjdbGTFfile argument in the STAR command.\n",
          "format": "path",
          "exists": true,
          "help_text": "type: `string`, multiple: False, direction: `input`, example: `transcriptome.gtf`"
        },
        "output": {
          "type": "string",
          "description": "Path to output directory. Corresponds to the --genomeDir argument in the STAR command.",
          "format": "path",
          "exists": true,
          "help_text": "type: `string`, multiple: False, required, direction: `output`, example: `/path/to/foo`"
        }
      },
      "required": [
        "genome_fasta",
        "output"
      ]
    },
    "genome indexing arguments": {
      "type": "object",
      "title": "Genome indexing arguments",
      "description": "No description",
      "properties": {
        "genomeSAindexNbases": {
          "type": "integer",
          "default": 14,
          "description": "Length (bases) of the SA pre-indexing string. Typically between 10 and 15.\nLonger strings will use much more memory, but allow faster searches. For small\ngenomes, the parameter {genomeSAindexNbases must be scaled down to\nmin(14, log2(GenomeLength)/2 - 1).\n",
          "help_text": "type: `integer`, multiple: False, default: `14`"
        }
      }
    },
    "nextflow input-output arguments": {
      "type": "object",
      "title": "Nextflow input-output arguments",
      "description": "Input/output parameters for Nextflow itself. Please note that both publishDir and publish_dir are supported but at least one has to be configured.",
      "properties": {
        "publish_dir": {
          "type": "string",
          "description": "Path to an output directory.",
          "format": "path",
          "help_text": "type: `string`, required, direction: `output`, example: `output/`"
        }
      },
      "required": [
        "publish_dir"
      ]
    }
  },
  "allOf": [
    {
      "$ref": "#/$defs/input_output"
    },
    {
      "$ref": "#/$defs/genome indexing arguments"
    },
    {
      "$ref": "#/$defs/nextflow input-output arguments"
    }
  ]
}
