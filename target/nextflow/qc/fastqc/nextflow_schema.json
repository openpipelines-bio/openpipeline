{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "fastqc",
  "description": "Fastqc component, please see https://www.bioinformatics.babraham.ac.uk/projects/fastqc/. This component can take one or more files (by means of shell globbing) or a complete directory.\n",
  "type": "object",
  "$defs": {
    "Dataset input": {
      "title": "Dataset input",
      "type": "object",
      "description": "Dataset input using nf-tower \"dataset\" or \"data explorer\". Allows for the input of multiple         parameter sets to initialise a Nextflow channel.",
      "properties": {
        "param_list": {
          "description": "Dataset input can either be a list of maps, a csv file, a json file, a yaml file, or simply a yaml                 blob. The names of the input fields (e.g. csv columns, json keys) need to be an exact match with the workflow input parameters.",
          "default": "",
          "format": "file-path",
          "mimetype": "text/csv",
          "pattern": "^\\S+\\.csv$"
        }
      }
    },
    "arguments": {
      "title": "Arguments",
      "type": "object",
      "description": "No description",
      "required": [
        "input",
        "output"
      ],
      "properties": {
        "mode": {
          "type": "string",
          "description": "The mode in which the component works",
          "help_text": "Type: `string`, multiple: `False`, default: `files`, choices: ``files`, `dir``. ",
          "enum": [
            "files",
            "dir"
          ],
          "default": "files"
        },
        "input": {
          "type": "string",
          "format": "path",
          "exists": true,
          "description": "Directory containing input fastq files",
          "help_text": "Type: `file`, multiple: `False`, required, example: `fastq_dir`. "
        },
        "output": {
          "type": "string",
          "format": "path",
          "exists": true,
          "description": "Output directory to write reports to",
          "help_text": "Type: `file`, multiple: `False`, required, default: `$id.$key.output`, example: `qc`. ",
          "default": "$id.$key.output"
        },
        "threads": {
          "type": "integer",
          "description": "Specifies the number of files which can be processed simultaneously",
          "help_text": "Type: `integer`, multiple: `False`. "
        }
      }
    },
    "nextflow input-output arguments": {
      "title": "Nextflow input-output arguments",
      "type": "object",
      "description": "Input/output parameters for Nextflow itself. Please note that both publishDir and publish_dir are supported but at least one has to be configured.",
      "required": [],
      "properties": {
        "publish_dir": {
          "type": "string",
          "description": "Path to an output directory",
          "help_text": "Type: `string`, multiple: `False`, required, example: `output/`. "
        }
      }
    }
  },
  "allOf": [
    {
      "$ref": "#/definitions/arguments"
    },
    {
      "$ref": "#/definitions/nextflow input-output arguments"
    }
  ]
}
