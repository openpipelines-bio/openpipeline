{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "cellranger_atac_mkfastq",
  "description": "Demultiplex raw sequencing data for ATAC experiments",
  "type": "object",
  "$defs": {
    "arguments": {
      "type": "object",
      "title": "Arguments",
      "description": "No description",
      "properties": {
        "input": {
          "type": "string",
          "description": "Path of Illumina BCL run folder.",
          "format": "path",
          "exists": true,
          "help_text": "type: `string`, multiple: False, required, direction: `input`, example: `/path/to/bcl`"
        },
        "csv": {
          "type": "string",
          "description": "The path to the simple layout sample sheet.",
          "format": "path",
          "exists": true,
          "help_text": "type: `string`, multiple: False, required, direction: `input`, example: `SampleSheet.csv`"
        },
        "lanes": {
          "type": "array",
          "description": "bcl2fastq option. Semicolon-delimited series of lanes to demultiplex. Use this if you have a sample sheet for an entire flow cell but only want to generate a few lanes for further 10x Genomics analysis.",
          "items": {
            "type": "string"
          },
          "help_text": "type: `array`, multiple: True, example: `1,3`"
        },
        "use_bases_mask": {
          "type": "array",
          "description": "bcl2fastq option. Use to clip extra bases off a read if you ran extra cycles for QC.",
          "items": {
            "type": "string"
          },
          "help_text": "type: `array`, multiple: True, example: `y50n,I6n,Y50n`"
        },
        "delete_undetermined": {
          "type": "boolean",
          "default": false,
          "description": "bcl2fastq option. Delete the Undetermined FASTQs generated by bcl2fastq. Useful if you are demultiplexing a small number of samples from a large flow cell.",
          "help_text": "type: `boolean`, default: `False`"
        },
        "barcode_mismatches": {
          "type": "integer",
          "default": 1,
          "description": "bcl2fastq option. Use this option to change the number of allowed mismatches per index adapter (0, 1, 2).",
          "help_text": "type: `integer`, multiple: False, default: `1`"
        },
        "output": {
          "type": "string",
          "default": "fastqs",
          "description": "The folder to store the demux results",
          "format": "path",
          "exists": true,
          "help_text": "type: `string`, multiple: False, required, default: `fastqs`, direction: `output`, example: `/path/to/output`"
        },
        "reports": {
          "type": "string",
          "description": "Reports directory",
          "format": "path",
          "exists": true,
          "help_text": "type: `string`, multiple: False, direction: `output`, example: `reports_dir`"
        }
      },
      "required": [
        "input",
        "csv",
        "output"
      ]
    },
    "nextflow input-output arguments": {
      "type": "object",
      "title": "Nextflow input-output arguments",
      "description": "Input/output parameters for Nextflow itself. Please note that both publishDir and publish_dir are supported but at least one has to be configured.",
      "properties": {
        "publish_dir": {
          "type": "string",
          "description": "Path to an output directory.",
          "format": "path",
          "help_text": "type: `string`, required, direction: `output`, example: `output/`"
        }
      },
      "required": [
        "publish_dir"
      ]
    }
  },
  "allOf": [
    {
      "$ref": "#/$defs/arguments"
    },
    {
      "$ref": "#/$defs/nextflow input-output arguments"
    }
  ]
}
