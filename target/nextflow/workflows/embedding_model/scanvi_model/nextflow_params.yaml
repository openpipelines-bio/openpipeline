# Inputs
input: # please fill in - example: "reference.h5mu"
modality: "rna"
# layer: "foo"
obs_target: # please fill in - example: "cell_type"
obs_batch_label: # please fill in - example: "sample"
# obs_size_factor: "foo"
# obs_categorical_covariate: ["foo"]
# obs_continuous_covariate: ["foo"]
unlabeled_category: "Unknown"
# var_hvg: "foo"
# var_gene_names: "foo"

# scVI and scANVI training options
# early_stopping: true
early_stopping_monitor: "elbo_validation"
early_stopping_patience: 45
early_stopping_min_delta: 0.0
# max_epochs: 123
reduce_lr_on_plateau: true
lr_factor: 0.6
lr_patience: 30.0

# Outputs
# output: "$id.$key.output.h5mu"
# output_compression: "gzip"
# output_scvi_model: "$id.$key.output_scvi_model"
# output_scanvi_model: "$id.$key.output_scanvi_model"

# Nextflow input-output arguments
publish_dir: # please fill in - example: "output/"
# param_list: "my_params.yaml"

# Arguments
