  docker.enabled = true
  def viash_temp = System.getenv("VIASH_TEMP") ?: "/tmp/"
  docker.runOptions = "-i -v ${baseDir}:${baseDir} -v $viash_temp:$viash_temp"
  process.container = "dataintuitive/viash"
  params {
    sync_test_resources__input = "s3://openpipelines-data"
    sync_test_resources__output = "resources_test"
    id = ""
    testScript = "run_test.sh"
    testResources = [ "run_test.sh" ]
    sync_test_resources {
      name = "sync_test_resources"
      container = "download_sync_test_resources"
      containerTag = "0.3.0rc2"
      containerRegistry = "ghcr.io"
      containerOrganization = "openpipelines-bio"
      command = "sync_test_resources"
      tests {
        isDefined = true
        testScript = "run_test.sh"
        testResources = [ "run_test.sh" ]
      }
      arguments {
        input {
          name = "input"
          otype = "--"
          required = false
          type = "string"
          direction = "Input"
          multiple = false
          multiple_sep = ":"
          value = "${params.sync_test_resources__input}"
          dflt = "s3://openpipelines-data"
          description = "Path to the S3 bucket to sync from."
        }
        output {
          name = "output"
          otype = "--"
          required = false
          type = "file"
          direction = "Output"
          multiple = false
          multiple_sep = ":"
          value = "${params.sync_test_resources__output}"
          dflt = "resources_test"
          description = "Path to the test resource directory."
        }
      }
    }
  }