name: fit_proportion_dynamics
namespace: "trajectory"
scope: "public"
description: |
  Fits a smoothing spline (`scipy.interpolate.UnivariateSpline`) of label proportion
  versus pseudotime to reveal which cell subpopulations expand or contract along a
  disease trajectory.

  This is a group-level component: every row is a group (a participant, donor or
  sample), not a cell. It takes two tables and writes two tables, and never opens a
  MuData - no cell-level data is involved.

    --input       group x label proportions
                  (`--output_csv` of `stats/calculate_label_proportions`)
    --pseudotime  one pseudotime value per group
                  (`--output_table` of `trajectory/palantir`)

  Both tables are joined on their identifier column; groups missing from either are
  dropped.

  This is step 4 of the BEYOND methodology (Green et al., Nature 2024).

keywords: [ trajectory, pseudotime, spline, dynamics, BEYOND ]
links:
  documentation: https://docs.scipy.org/doc/scipy/reference/generated/scipy.interpolate.UnivariateSpline.html
references:
  doi: 10.1038/s41586-024-07871-6
license: MIT
authors:
  - __merge__: /src/authors/marilyn_degraeve.yaml
    roles: [ author ]

argument_groups:
  - name: Inputs
    arguments:
      - name: "--input"
        alternatives: ["-i"]
        type: file
        direction: input
        required: true
        description: |
          CSV with the group x label proportion matrix: an identifier column plus one
          numeric column per label.
        example: proportions.csv

      - name: "--pseudotime"
        type: file
        direction: input
        required: true
        description: |
          CSV with the same identifier column plus a pseudotime column
          (`--pseudotime_column`). Extra columns are ignored.
        example: pseudotime.csv

      - name: "--id_column"
        type: string
        required: false
        description: |
          Identifier column shared by both tables. Defaults to the first column of
          `--input`.
        example: "participant_id"

      - name: "--pseudotime_column"
        type: string
        default: "palantir_pseudotime"
        required: false
        description: Column of `--pseudotime` holding the pseudotime values.

  - name: Arguments
    arguments:
      - name: "--lam"
        type: double
        default: 0.6
        min: 0.0
        required: false
        description: |
          Smoothing regularisation penalty. The spline smoothing factor is
          `lam * n_groups`; higher values produce smoother fits.

      - name: "--n_pseudotime_bins"
        type: integer
        default: 100
        min: 10
        required: false
        description: Number of evenly-spaced pseudotime values on which the fitted curve is evaluated.

      - name: "--min_groups"
        type: integer
        default: 5
        min: 4
        required: false
        description: |
          Minimum number of groups required to fit. A cubic spline needs at least 4
          points; fewer groups than this is an error rather than a silent bad fit.

  - name: Outputs
    arguments:
      - name: "--output"
        alternatives: ["-o"]
        type: file
        direction: output
        required: true
        description: |
          Long-format CSV of the fitted curves, one row per (label, pseudotime bin):
          `label`, `pseudotime`, `proportion_fitted`.
        example: dynamics.csv

      - name: "--output_stats"
        type: file
        direction: output
        required: false
        description: |
          CSV with one row per label: `label`, `peak_pseudotime`, `r_squared`,
          `p_value`, `n_groups`.
        example: dynamics_stats.csv

resources:
  - type: python_script
    path: script.py
  - path: /src/utils/setup_logger.py
  - path: /src/utils/group_table.py

test_resources:
  - type: python_script
    path: test.py
  - path: /src/utils/setup_logger.py
  - path: /src/utils/group_table.py

engines:
  - type: docker
    image: python:3.12-slim
    setup:
      - type: apt
        packages:
          - procps
      - type: python
        packages:
          - pandas
          - numpy
          - scipy
    test_setup:
      - type: python
        __merge__: [/src/base/requirements/viashpy.yaml, .]

runners:
  - type: executable
  - type: nextflow
    directives:
      label: [midcpu, midmem, middisk]
