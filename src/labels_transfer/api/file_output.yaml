type: "file"
description: "The query data in .h5mu format with predicted labels transfered from the reference."
info:
  label: "Output data"
  file_format:
    type: h5mu
    mod:
      rna:
        description: "Modality in AnnData format containing RNA data."
        required: true
        obs:
          - type: "string"
            name: "target_pred"
            description: "Transfered labels. Could have any name, specified in `targets` argument \
            \ with suffix specified in `output_obs_suffix` argument."
            required: true
          - type: "double"
            name: "target_uncertainty"
            description: "The uncertainty of predictions for labels."
            required: true
        obsm:
          - type: "double"
            name: "X_integrated_scanvi"
            description: "The embedding used for the classifier's inference. Could have any name,\
            \ specified by `query_obsm_key` argument."
            required: false
        uns:
          - type: "string"
            name: "labels_transfer"
            description: "The name of the uns slot containing the information about labels transfer. \
            \ Can have any name, specified by `output_uns_key` argument."
            required: true
